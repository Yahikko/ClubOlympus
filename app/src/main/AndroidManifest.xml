<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClubOlympus"
        tools:targetApi="31">
        <activity
            android:name=".AddMemberActivity"
            android:exported="false"
            android:label="Add Member">
            <!-- <meta-data.../> для создания кнопки возврата..-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--ContentProvirder также необходимо указать в манифесте-->
        <!--Параметр exported - виден ли провайдер для других приложений-->
        <provider
            android:authorities="com.example.clubolympus"
            android:name="com.example.clubolympus.data.OlympusContentProvider"
            android:exported="false"/>
    </application>

</manifest>